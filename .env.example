## Frontend environment example for Vite
# Copy this file to `.env.frontend` and fill the real keys. DO NOT commit `.env.frontend`.

# Supabase public anon key (safe to use in frontend)
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Optional: override API base URLs if running backend in container or different host
VITE_API_BASE_URL=http://localhost:8000
